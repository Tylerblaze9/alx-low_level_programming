#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Aways 0 (Sucess)
 *
 */
int main(void)
{
	int lowercase = 'a';

	while (lowercase <= 'z')
	{
		if (lowercase == 'a' || lowercase == 'q')
		{
			lowerCase += 1;
		}
	        eles
		{
			putchar(lowerCase);
			lowerCase += 1;
		}
	}
	putchar('\n');

	return (0);
}
